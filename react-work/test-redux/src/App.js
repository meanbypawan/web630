import logo from './logo.svg';
import './App.css';
import { useSelector } from 'react-redux';
import First from './First';
import { Student } from './Student';

function App() {
  //  let {counter} = useSelector((state)=>state.data);
  //  let {evenCounter} = useSelector((state)=>state.evenData);
  //  console.log(counter);
   return <>
      <Student/>
   </>

}

export default App;
